<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ar8ee">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:sharedUserId="android.uid.system"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UI.addPost"
            android:parentActivityName=".UI.InnerActivity" />
        <activity android:name=".UI.MainActivity" />
        <activity
            android:name=".UI.ResetPasswordActivity"
            android:parentActivityName=".UI.LoginActivity" />
        <activity android:name=".UI.Chat" />
        <activity
            android:name=".UI.LoginActivity"
            android:parentActivityName=".UI.MainActivity" />
        <activity android:name=".UI.InnerActivity" />
        <activity
            android:name=".UI.RegisterActivity"
            android:parentActivityName=".UI.MainActivity" />
        <activity android:name=".UI.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.example.ar8ee.notification.MyFirebaseIdService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name="com.example.ar8ee.notification.MyFirebaseMessaging"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name="com.example.ar8ee.notification.MyIntentService"
            android:exported="false" />
    </application>

</manifest>